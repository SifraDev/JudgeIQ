1. Backend Update (/api/research): Modify the OpenAI system prompt. The spoken_script is currently too long and monologic. Change the prompt to generate a very short, highly conversational assistant response (max 2 sentences).Format required: "I have compiled the research on Judge [Name]. [One punchy, interesting key insight]. What specific area of their record would you like to explore?"
2. Frontend Update (Strict State Transition): Currently, the UI jumps to ResultsView prematurely because Firecrawl finishes fast but the LLM synthesis is still processing, causing empty cards to flash. Update the frontend logic (likely in useElevenLabs.ts or ElevenLabsCinematicView.tsx) to ensure the app STRICTLY remains in the PROCESSING state (showing the fire orb) until the entire /api/research fetch request is fully resolved and the atomic state update (setResearchData) is complete. The view should only flip to results when the arrays for tendencies and biases are fully populated and ready to render.
Constraints:

Do not change any CSS or visual layouts.
Keep the atomic state update introduced in Task 13.

UX Polish, Conversational Voice, and Real-Time Pipeline Logging
1. Backend Architecture Update (api-server): We need to split the /api/research logic so the frontend can track the exact speed of Firecrawl versus the LLM synthesis.

Ensure the original POST /api/firecrawl/search exists and works (returns raw scraped data).
Create a new endpoint POST /api/synthesize. This endpoint should accept the raw data from Firecrawl in the request body, pass it to the OpenAI integration (gpt-4o-mini), and return the strict JSON structure (spoken_script, tendencies, biases).
Prompt Update: Inside /api/synthesize, modify the OpenAI system prompt so the spoken_script is extremely short and conversational (max 2 sentences). Format exactly like this: "I have compiled the research on Judge [Name]. [One punchy, interesting key insight]. I've populated the dashboard. What specific area would you like to explore?"
2. Frontend Update (useElevenLabs.ts): Update the firecrawl_search client tool to execute the two steps sequentially and log their real times:

Step 1: Start a timer (performance.now()). Fetch /api/firecrawl/search. Stop the timer. Call addLog(\Firecrawl extraction complete in ${timeInSeconds}s`, 'success')`.
Step 2: Call addLog('Synthesizing judicial profile via LLM...', 'system'). Fetch /api/synthesize, passing the Firecrawl results.
Step 3: Perform the atomic state update (setResearchData) with the final JSON.
Step 4: Return JSON.stringify({ summary: synthesizeData.spoken_script }) to the agent.
3. Strict Loading State: Ensure the ElevenLabsCinematicView.tsx logic strictly maintains the PROCESSING state (the fire orb) during BOTH fetch requests. It must absolutely not transition to the results view until setResearchData is finished and the arrays are fully populated.
Constraints:

Do not change any visual layouts, CSS, or Framer Motion properties. Only update the data flow and logging.